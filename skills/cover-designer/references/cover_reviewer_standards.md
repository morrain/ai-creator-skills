# Cover Reviewer Standards (封面设计盲审质检标准)

本标准供独立 SubAgent 盲审引擎 (`blind-reviewer`) 对 `cover-designer` 技能导出的 `assets/cover.md` 进行严格质检打回。

---

## 1. 结构完整性门控 (Hard Schema Gates)

- 必须输出标准的 `assets/cover.md` 格式：
  - 必须包含 `# 封面设计：[爆款主标题]` 标题。
  - 必须包含 `## 封面元数据`（指明目标平台、核心主题、互动范式）。
  - 必须包含 `## 🔵 中文确认版 封面视觉与爆款排版设计`（明确爆款主标题、评论引导标记、物理隐喻构件与 IP 姿态）。
  - 必须包含 `## 🟢 英文生图版 Prompt (按平台区分)`（分块提供小红书 3:4、微信 16:9、视频号 9:16 等专属生图 Prompt）。

---

## 2. CTR 点击率与视觉冲击力审查 (CTR & Visual Impact Gate)

- 🔤 **爆款 Hook 标题与超大字号一致性**：若上游传入了指定的标题（如正文 H1），必须完全复用，绝对禁止缩减、篡改或重新提炼（不一致直接打回 `[REJECT]`）！若未指定，则需提炼 10~14 字内具备强烈痛点/悬念感的 Hook。无论何种情况，主标题字号必须标记为 $\ge 120px$ 极粗体。副标题字号必须标记为 $\ge 80px$ 并带有高对比度底色或包边，如果副标题显得太小或看不清，打回 `[REJECT]`！
- 🚫 **反标题党与低质引流审查 (Anti-Clickbait Gate)**：
  - ❌ **硬性打回项**：若封面方案的标题或互动引导语中包含了“震惊”、“必看”、“男默女泪”、“全网最全”等廉价极端词汇，判定为“低质标题党”，直接打回 `[REJECT]`！
  - ❌ **硬性打回项**：若包含“关注获取源码”、“点赞领资料”等强迫诱导互动，判定为“低质导流”，直接打回 `[REJECT]`！
- 🎯 **视觉焦点与强概念隐喻 (Strong Conceptual Metaphor)**：审查画面是否具备清晰的视觉主次与充足留白，**且必须使用强概念视觉隐喻来精准表达核心含义**。绝不能仅仅画一个普通的背景或字面意思上的静物（如讲“内存泄漏”只画一台电脑）。若缺乏能吸引人的深度隐喻，或强行硬塞滑稽/死板的比喻，必须打回！❌ **硬性打回项**：若画面描述了左右分割场景、多故事线散乱堆叠或 3 个以上并列构件，判定为“视觉杂乱无焦点”，直接打回 `[REJECT]`！
- 🚫 **文字防杂乱审查 (Anti-Clutter Gate)**：英文生图 Prompt 必须保证文字主次分明、服务于核心主题。❌ **硬性打回项**：若 Prompt 中包含了大量零碎无意义的贴纸字符、假字乱码或分散视觉注意力的无用文本噪点，判定为“视觉干扰严重”，直接打回 `[REJECT]`！
- 🚫 **海报与导流牛皮癣隔离审查 (Cover vs. Poster Gate)**：
  - ❌ **硬性打回项 1**：若封面方案中包含了“留言/私信领资料”、“关注领福利”等私域导流/广告 CTA 贴纸，判定为“低质导流牛皮癣”，直接打回 `[REJECT]`！
  - ❌ **硬性打回项 2**：若封面包含了多段总结卡片 A/B、要点清单堆叠，判定为“错把海报当封面”，打回 `[REJECT]`（长文总结卡片属于海报 `poster-designer` 范畴）！
- 🤖 **IP Mascot 姿态**：IP Mascot 必须呈现具备发问/探查/指引张力的定格动作，严禁单向僵硬站立或无意义卖萌。

---

## 3. 评论引力与互动标记按需审查 (Content-Driven Comment Engagement Gate)

- 💬 **评论互动钩子条件触发**：
  - **若内容具备天然互动切口**（如对立路线、避坑多选、悬念猜想、痛点吐槽或认知差）：封面设计必须严格匹配 `references/engagement_recipes.md` 中的爆款范式之一，并在中文方案与英文 Prompt 中配置评论区引导标记（如 `"👉 评论区留下你的观点"`）。
  - **若内容为硬核原理/干货指南/流程解剖（无天然评论切口）**：**充许且鼓励将评论引导标记设为 `null`**。盲审引擎严禁强求硬塞机械化的套路引导语，应重点审查 H1 爆款标题的干货吸引力与 2D 物理隐喻的精妙度！强塞生硬套路者打回！

---

## 4. 平台画幅与生图 Prompt 合规性 (Multi-Platform Prompt Gate)

- 📐 **画幅标记精准匹配**：
  - 小红书版 Prompt 必须包含 3:4 比例标记（`3:4 aspect ratio` / `--ar 3:4`）。
  - 微信公众号版 Prompt 必须包含 16:9 比例标记，**并且必须在 Prompt 中显式包含绝对垂直居中安全区的声明（`strictly placed in the absolute vertical center safe zone`）**。若漏掉安全区约束导致裁切风险，打回 `[REJECT]`！
  - 视频号/抖音版 Prompt 必须包含 9:16 竖屏比例标记。
- 🎨 **美学风格多样性与自适应**：生图 Prompt 须根据文章/剧本文风与主题自适应匹配画风（暖米白手绘、暗色光影、明亮杂志几何、网格蓝图草图、戏剧场景插画等）。❌ **打回项**：若生图 Prompt 无视主题文风一律套用单一模板画风，判定为“千篇一律同质化”，打回 `[REJECT]`！
